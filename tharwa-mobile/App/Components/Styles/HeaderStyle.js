import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    width: '100%',
    position: 'relative'
  },
  leftButton: {
    position: 'absolute',
    left: 5
  },
  body: {
    flex: 1,
    alignItems: 'center'
  }
})
