import React from "react";
import { shallow } from "enzyme";

import ConfirmationMethodPrompt from "../ConfirmationMethodPrompt";

describe("ConfirmationMethodPromt", () => {
  it("should rendered without crashing", () => {
    // const wrapper = shallow(<ConfirmationMethodPrompt />);
    // expect(wrapper.find("ConfirmationMethodPromt")).toHaveLength(1);
  });
});
